debug
target
logs

# Host-specific cargo config (machine paths for local cross-compilation without Docker)
.cargo/config.toml

# Sysroot bundles — large binaries generated by bundle-sysroots.sh, consumed by Docker
docker/sysroots/

